import "./Header.css";

function Header() {
  const name = "Add Employee Details";
  return (
    <div className="header-container">
      <h1>{name}</h1>
    </div>
  );
}

export default Header;
